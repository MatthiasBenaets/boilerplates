id: ansible-docker
namespace: ansible
description: Install Docker Engine on Debian-based system

inputs:
  - id: hosts
    displayName: "Hosts (comma separated)"
    type: STRING
    defaults: simlab

tasks:
  - id: ansible_task
    type: io.kestra.plugin.ansible.cli.AnsibleCLI
    ansibleConfig: |
      [defaults]
      interpreter_python = auto_silent
    namespaceFiles:
      enabled: true
      include:
        - inventory.ini
        - playbooks/docker.yml
        - id_rsa
    taskRunner:
      type: io.kestra.plugin.scripts.runner.docker.Docker
      pullPolicy: IF_NOT_PRESENT
      user: "1000"
    containerImage: cytopia/ansible:latest-tools
    commands:
      - ansible-playbook -i inventory.ini --key-file id_rsa --extra-vars "target={{ inputs.hosts }}" playbooks/docker.yml
    env:
      ANSIBLE_HOST_KEY_CHECKING: "False"