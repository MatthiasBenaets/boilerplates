# All proxmox hosts
[virtual:children]
vm
lxc

# All Proxmox Linux VMs
[vm]
media

# All Proxmox Debian LXCs
[lxc]
network
proxy
website
cloud
misc

# Temporary host
[temp]
media

[vm:vars]
ansible_ssh_user=root

[lxc:vars]
ansible_ssh_user=root
